pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Add steps to build your project
                sh 'npm install'
            }
        }
        stage('Test') {
            steps {
                // Add steps to run tests
                sh 'npm test'
            }
        }
        stage('Deploy') {
            steps {
                // Add steps to deploy your application
                // sh 'npm deploy'
            }
        }
    }
}

